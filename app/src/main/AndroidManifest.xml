<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yunkai.learnfruit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PineappleActivity"></activity>
        <activity android:name=".PearActivity" />
        <activity android:name=".GrapesActivity" />
        <activity android:name=".CherryActivity" />
        <activity android:name=".WatermelonActivity" />
        <activity android:name=".AppleActivity" />
        <activity android:name=".BananaActivity" />
        <activity android:name=".MainActivity"
            android:configChanges="locale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>